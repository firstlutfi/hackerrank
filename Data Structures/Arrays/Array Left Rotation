function rotLeft($a, $d) {
    $temp = array();

    for($i=$d; $i<count($a); $i++){
        array_push($temp, $a[$i]);
    }

    for($i=0; $i<$d; $i++){
        array_push($temp, $a[$i]);
    }
    
    return $temp;

}